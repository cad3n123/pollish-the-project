# Slideshow Website

This project displays a simple slideshow of images using HTML, CSS, and JavaScript. To enable basic progressive enhancement, the script replaces the `no-js` class on the `<html>` element with `js`.
